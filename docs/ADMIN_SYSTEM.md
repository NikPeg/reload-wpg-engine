# 🔐 Система администрирования WPG Engine

## Обзор

Система администрирования WPG Engine обеспечивает управление игрой с гибкой системой ролей и расширенными возможностями для администраторов.

## 🎯 Принципы работы

### 1. **Назначение администраторов**

Система использует два способа определения администраторов:

#### A. **Админ через переменную окружения**

```bash
# В файле .env

# Пользователь-админ (положительное значение)
TG_ADMIN_ID=123456789

# Или админский чат (отрицательное значение)
TG_ADMIN_ID=-1001234567890
```

**Режимы работы TG_ADMIN_ID:**

- **Положительное значение** - ID конкретного пользователя, который является админом везде
- **Отрицательное значение** - ID чата/группы, где все сообщения считаются админскими

#### B. **Автоназначение первого игрока**

- Первый зарегистрированный игрок в игре автоматически становится администратором
- Если в игре нет админов, следующий игрок также получает права админа

### 2. **Роли пользователей**

```python
class PlayerRole(str, Enum):
    ADMIN = "admin"      # Полные права управления
    PLAYER = "player"    # Обычный игрок
    OBSERVER = "observer" # Наблюдатель (только чтение)
```

## 🛠️ Компоненты системы

### 1. **Утилиты администрирования** (`wpg_engine/core/admin_utils.py`)

#### `determine_player_role(telegram_id, game_id, db, chat_id)`

Определяет роль игрока на основе:
1. Проверки, является ли сообщение из админского чата
2. Автоназначения первого игрока
3. Проверки существующих админов в игре

#### `is_admin(telegram_id, db, chat_id)`

Быстрая проверка прав администратора с учетом:
- Админского чата (если сообщение из чата)
- Роли в базе данных

#### `is_admin_chat(chat_id)`

Проверяет, является ли чат админским на основе `TG_ADMIN_ID`.

### 2. **Конфигурация** (`wpg_engine/config/settings.py`)

```python
class TelegramSettings(BaseSettings):
    admin_id: int | None = Field(
        default=None,
        description="Admin ID (positive for user, negative for chat)"
    )
    
    def is_admin_chat(self) -> bool:
        """Check if admin_id represents a chat"""
        return self.admin_id is not None and self.admin_id < 0
    
    def is_admin_user(self) -> bool:
        """Check if admin_id represents a user"""
        return self.admin_id is not None and self.admin_id > 0
```

### 3. **Интеграция с регистрацией** (`wpg_engine/adapters/telegram/handlers/registration.py`)

При регистрации нового игрока:
1. Определяется роль через `determine_player_role()`
2. Создается игрок с соответствующей ролью
3. Показывается специальное сообщение для админов

### 4. **Проверка прав в командах** (`wpg_engine/adapters/telegram/handlers/admin.py`)

Все админские команды используют:

```python
if not await is_admin(user_id, game_engine.db, message.chat.id):
    await message.answer("❌ У вас нет прав администратора.")
    return
```

## 📋 Админские команды

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню и панель администратора |
| `/game_stats` | Статистика игры |
| `/update_game` | Изменить параметры игры |
| `/restart_game` | Перезапустить игру (полная очистка) |
| `/event` | Отправить сообщение всем игрокам |

### Управление странами

| Команда | Описание |
|---------|----------|
| `/delete_country` | Удалить страну из игры |
| `/edit_country` | Редактировать параметры страны |
| `/add_example` | Добавить страну в примеры |

### Примеры стран

Система примеров позволяет администраторам создавать готовые страны для новых игроков.

#### Создание примера

```bash
# Админ использует команду
/add_example

# Бот показывает список всех стран в игре
# Админ отвечает на сообщение со страной, которую хочет сделать примером
# Бот добавляет страну в примеры
```

#### Использование примеров игроками

1. **Просмотр примеров:**
   ```bash
   /examples
   ```
   Показывает все доступные примеры стран с полными характеристиками

2. **Выбор примера:**
   - Игрок во время регистрации может использовать `/examples`
   - Бот показывает примеры с маркером `[EXAMPLE:X]`
   - Игрок отвечает на сообщение с примером словами "выбрать" или "выбираю"
   - Выбранная страна присваивается игроку
   - Пример удаляется из базы данных (становится недоступен для других)

3. **Особенности:**
   - Выбор примера работает на **любом этапе регистрации**
   - Не требует отдельного состояния FSM
   - Атомарная операция - пример удаляется сразу после выбора
   - Предотвращает дублирование стран

#### Технические детали примеров

**Модель Example** (`wpg_engine/models/example.py`):

```python
class Example(Base):
    __tablename__ = "examples"
    
    country_id: Mapped[int]      # Страна-пример
    game_id: Mapped[int]          # Игра, к которой относится
    created_by_id: Mapped[int]    # Админ, создавший пример
```

**Обработчик выбора** (`registration.py`):

```python
class IsExampleSelection(Filter):
    """Фильтр для проверки выбора примера"""
    # Проверяет reply, состояние регистрации, маркер [EXAMPLE:X]
    # и ключевые слова "выбрать"/"выбираю"

async def process_example_selection(message, state):
    """Обработка выбора примера на любом этапе регистрации"""
    # Извлечение ID примера, проверка доступности
    # Создание/обновление игрока, удаление примера
```

## 🔧 Настройка

### 1. **Добавление админа через .env**

```bash
# Один пользователь-админ
TG_ADMIN_ID=123456789

# Админский чат
TG_ADMIN_ID=-1001234567890
```

### 2. **Получение Telegram ID**

Для получения ID:

1. **Для пользователя:** Напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram
2. **Для чата:** Добавьте бота в группу, ID чата будет в логах (отрицательное число, начинается с `-100`)

### 3. **Тестирование системы**

```bash
# Запуск тестов системы администрирования
python -m pytest tests/test_admin_system.py -v

# Тесты примеров стран
python -m pytest tests/test_examples.py -v
python -m pytest tests/test_registration_with_examples.py -v

# Проверка настроек
python -m pytest tests/test_settings.py -v
```

## 🎮 Сценарии использования

### Сценарий 1: Новая игра без предустановленных админов

1. Первый игрок регистрируется → получает роль `ADMIN`
2. Последующие игроки → получают роль `PLAYER`

### Сценарий 2: Игра с админским чатом

1. Админы создают чат и добавляют в него бота
2. Устанавливают `TG_ADMIN_ID=-1001234567890` (ID чата)
3. Все пользователи, пишущие из этого чата, имеют права админа
4. Это удобно для команды из нескольких человек

### Сценарий 3: Использование примеров стран

1. **Подготовка:**
   - Админ создает несколько хорошо проработанных стран
   - Добавляет их в примеры командой `/add_example`

2. **Регистрация игрока:**
   - Новый игрок начинает регистрацию `/register`
   - Видит подсказку об использовании `/examples`
   - Может в любой момент посмотреть примеры
   - Отвечает на понравившуюся страну "выбираю"
   - Сразу получает готовую страну и завершает регистрацию

3. **Преимущества:**
   - Новые игроки быстрее начинают играть
   - Снижается нагрузка на админа по одобрению заявок
   - Игроки видят примеры хороших заявок
   - Админ контролирует баланс через примеры

### Сценарий 4: Смешанный режим

1. Админ из `.env` → `ADMIN` везде
2. Админы из чата → `ADMIN` только в чате
3. Первый игрок (если нет админов) → `ADMIN`
4. Остальные → `PLAYER`

## 🔒 Безопасность

- ✅ Все админские команды защищены проверкой прав
- ✅ Роли хранятся в базе данных
- ✅ Поддержка множественных админов через чаты
- ✅ Автоматическое назначение при отсутствии админов
- ✅ Валидация Telegram ID
- ✅ Атомарность операций с примерами

## 🚀 Расширение системы

### Добавление новых ролей

1. Обновить `PlayerRole` enum в `wpg_engine/models/player.py`
2. Добавить логику в `determine_player_role()` в `admin_utils.py`
3. Создать соответствующие проверки прав
4. Обновить команды и интерфейс

### Добавление админских команд

```python
from wpg_engine.core.admin_utils import is_admin

async def my_admin_command(message: Message, game_engine: GameEngine):
    """Пример новой админской команды"""
    if not await is_admin(message.from_user.id, game_engine.db, message.chat.id):
        await message.answer("❌ У вас нет прав администратора.")
        return
    
    # Логика админской команды
    await message.answer("✅ Команда выполнена!")

# Регистрация команды
dp.message.register(my_admin_command, Command("my_command"))
```

## 📝 Примеры кода

### Проверка прав с учетом чата

```python
# В обработчике сообщения
is_user_admin = await is_admin(
    telegram_id=message.from_user.id,
    db=db,
    chat_id=message.chat.id  # Передаем ID чата!
)
```

### Получение роли при регистрации

```python
role = await determine_player_role(
    telegram_id=user_id,
    game_id=game_id,
    db=session,
    chat_id=message.chat.id  # Для админских чатов
)

if role == PlayerRole.ADMIN:
    await message.answer("👑 Вы назначены администратором!")
```

### Работа с примерами

```python
from wpg_engine.models import Example

# Создание примера (команда /add_example)
example = Example(
    country_id=country.id,
    game_id=game.id,
    created_by_id=admin_player.id
)
db.add(example)
await db.commit()

# Получение всех примеров для игры
result = await db.execute(
    select(Example)
    .where(Example.game_id == game_id)
    .options(selectinload(Example.country))
)
examples = result.scalars().all()

# Удаление примера после выбора
await db.delete(example)
await db.commit()
```

## 🔗 Связанные компоненты

- **Models:** `wpg_engine/models/player.py`, `wpg_engine/models/example.py`
- **Admin utilities:** `wpg_engine/core/admin_utils.py`
- **Settings:** `wpg_engine/config/settings.py`
- **Handlers:** 
  - `wpg_engine/adapters/telegram/handlers/admin.py` - админские команды
  - `wpg_engine/adapters/telegram/handlers/registration.py` - регистрация и выбор примеров
  - `wpg_engine/adapters/telegram/handlers/player.py` - команда `/examples`
- **Tests:**
  - `tests/test_admin_system.py` - тесты админской системы
  - `tests/test_examples.py` - тесты примеров
  - `tests/test_registration_with_examples.py` - интеграционные тесты

---

**Система администрирования готова к использованию! 🎮**
